# ⚠️  Backend Environment Variables - For Local Development
# ⚠️  Do NOT use these values in production!

# Database
# For local development with exposed port
DATABASE_URL=postgres://lifetrack:CHANGE_PASSWORD@localhost:8100/lifetrack?sslmode=disable

# ⚠️  REQUIRED: JWT Secret (min 32 chars)
# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_THIS_GENERATE_WITH_OPENSSL_RAND

# Server
PORT=8080
ENV=development
