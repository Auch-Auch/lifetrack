# ⚠️  SECURITY WARNING: Never commit .env files to version control!
# ⚠️  All values are REQUIRED - no defaults are provided for security
# ⚠️  Generate strong, unique values for production deployments

# ============================================================================
# Database Configuration
# ============================================================================
POSTGRES_DB=lifetrack
POSTGRES_USER=lifetrack
# ⚠️  REQUIRED: Use a strong password (min 16 chars, mixed case, numbers, symbols)
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_MIN_16_CHARS

# ============================================================================
# Backend Configuration
# ============================================================================
# ⚠️  REQUIRED: JWT secret for signing tokens (min 32 chars)
# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_THIS_TO_RANDOM_SECRET_MIN_32_CHARS

# Application environment (production/development)
ENV=production

# ============================================================================
# Telegram Bot Configuration
# ============================================================================
# ⚠️  REQUIRED: Get from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=CHANGE_THIS_GET_FROM_BOTFATHER

# ⚠️  REQUIRED: Generate AFTER database is running
# Generate with: cd backend && JWT_SECRET=your-secret DATABASE_URL=postgres://... go run cmd/generate-service-token/main.go telegram-bot
SERVICE_JWT=CHANGE_THIS_GENERATE_AFTER_DB_RUNNING

# API URL for frontend (used by Next.js)
NEXT_PUBLIC_API_URL=/query

# Caddy ports (optional, defaults to 80/443)
CADDY_PORT=80
CADDY_HTTPS_PORT=443

# ============================================================================
# LLM Configuration (Optional)
# ============================================================================
MODEL_N_THREADS=8
MODEL_N_GPU_LAYERS=0  # Set to > 0 if you have GPU support

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Generate secure passwords and secrets using the commands above
# 3. Get Telegram bot token from @BotFather
# 4. Start database: docker-compose up -d postgres
# 5. Generate SERVICE_JWT using the command shown above
# 6. Update ALLOWED_ORIGINS with your actual domain(s)
# 7. Start all services: docker-compose up -d
